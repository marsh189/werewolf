/* =============================================================================
   Socket Constants

   Shared defaults for Socket.IO emit/ack behaviors.
============================================================================= */

/* ---------------------------------------------------------------------------
   SOCKET_ACK_TIMEOUT_MS

   Default timeout for request/ack events where the server is expected to reply.
--------------------------------------------------------------------------- */
export const SOCKET_ACK_TIMEOUT_MS = 5000;
