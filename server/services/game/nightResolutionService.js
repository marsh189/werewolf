/* =============================================================================
   Night Resolution Service (Facade)

   Kept for backwards-compatible imports. The implementation lives in:
   - `server/services/game/nightResolution/nightResolutionService.js`
============================================================================= */

export * from './nightResolution/nightResolutionService.js';

